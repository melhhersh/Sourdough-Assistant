OPENROUTER_API_KEY=       # fallback LLM key (set in Vercel env vars, never committed)
OPENAI_API_KEY=           # for text-embedding-3-small only
ARIZE_SPACE_ID=           # from Arize AX Space Settings
ARIZE_API_KEY=            # from Arize AX Space Settings
ARIZE_PROJECT_NAME=sourdough-assistant
RATE_LIMIT_MAX=10         # max messages per session on fallback key
